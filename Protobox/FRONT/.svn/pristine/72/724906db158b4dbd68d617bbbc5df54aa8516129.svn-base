import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { NavbarliComponent } from './navbarli.component';

describe('NavbarliComponent', () => {
  let component: NavbarliComponent;
  let fixture: ComponentFixture<NavbarliComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ NavbarliComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(NavbarliComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
