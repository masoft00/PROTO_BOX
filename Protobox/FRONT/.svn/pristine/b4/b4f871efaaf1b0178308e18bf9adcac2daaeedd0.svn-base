import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { Navbarli2Component } from './navbarli2.component';

describe('Navbarli2Component', () => {
  let component: Navbarli2Component;
  let fixture: ComponentFixture<Navbarli2Component>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ Navbarli2Component ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(Navbarli2Component);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
