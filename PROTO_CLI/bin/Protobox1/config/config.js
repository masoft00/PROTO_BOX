const config = {
    URL: 'http://127.0.0.1',
    PORT: 3000,
    DB: {
        //MongoDB configuration for mongo DB
        HOST: '127.0.0.1',
        PORT: '27017',
        DATABASE: 'Protobox'
    }
}
module.exports = config